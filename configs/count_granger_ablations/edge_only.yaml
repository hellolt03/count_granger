ablation:
  name: edge_only
  description: Use directional edge surprise only; remove residual score from detection.
granger:
  residual_score_weight: 0.0
  edge_score_weight: 1.0
detect:
  score_component: edge
